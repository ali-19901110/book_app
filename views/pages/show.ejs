  
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Book Collection</title>
    <link rel="stylesheet" href="styles/base.css">
    <link rel="stylesheet" href="styles/reset.css">
    <link rel="stylesheet" href="styles/modules.css">


  </head>
  <body>
    <header>
      <%- include('../layout/header') %> 
    </header>
    <main>
      <div>
        <% searchResults.forEach( book => { %>
          <img src='<%=book.img.thumbnail  %>'/>
            <h3><%= book.title%></h3>
            <h4><%= book.authers %></h4>
            <h5><%= book.isbn%></h5>
            <p><%= book.description %></p> 
            
            <section>
              <h2>Add a new Book</h2>
              <form method="post" action="/books">
                  <legend>Add Book</legend>
                  <input type="text" name="title" placeholder="title" value="<%=book.title%>" />
                  <input name="description"  placeholder="Add Description" value="<%=book.description%>">
                  <input type="text" name="isbn" placeholder="Category" value="<%= book.isbn%>"/>
                  <input type="text" name="img" placeholder="Status" value="<%=book.img.thumbnail  %>"/>
                  <div><button type="submit">Save</button></div>
              </form>
          </section>
        <% }) %>
      </div>
   
    </main>
  </body>
</html>